/** Channel-change transition (ms) */
export const DISSOLVE_MS = 80;
export const STATIC_HOLD_MS = 300;
export const FADE_IN_MS = 200;
export const TOTAL_TRANSITION_MS = DISSOLVE_MS + STATIC_HOLD_MS + FADE_IN_MS;

export const OSD_VISIBLE_MS = 2500;
export const SCANLINE_REST = 0.04;
export const SCANLINE_PEAK = 0.12;
