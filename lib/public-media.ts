/**
 * URL prefix for mentor portraits on disk: `public/media/mentors/{slug}.png`.
 * Slug is derived from the display name when `image` is omitted in `data/about.json`.
 */
export const PUBLIC_MENTOR_IMAGE_BASE = "/media/mentors";
